export default class StudioRegModel {
  constructor(studioName, studioPlace, moderatorPhoneNumber) {
    (this.Name = studioName),
      (this.Place = studioPlace),
      (this.ModeratorPhoneNumber = moderatorPhoneNumber);
  }
}
